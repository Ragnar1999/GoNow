---
kind: frontend_style
name: No frontend styling system present
category: frontend_style
scope:
    - '**'
---

This repository contains only backend Python code (under `backend/app/`) and a `.gitignore` file. There are no CSS, SCSS, Tailwind configuration, theme files, or any other frontend styling assets in the codebase. The project appears to be a backend-only GoNow API with no frontend implementation, so the `frontend_style` category does not apply.